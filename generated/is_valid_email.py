# MIT License
# Copyright (c) 2026 axiom
# See LICENSE file for details.

# AXIOM GENERATED - DO NOT EDIT
# This file was generated by Axiom from: is_valid_email.axiom
# Spec: is_valid_email v1.0.0
# To modify this code, edit the spec file and run 'axiom build'
import re


def is_valid_email(email: str) -> bool:
    if not email or email.count("@") != 1:
        return False

    local, domain = email.split("@")

    if not local or not domain:
        return False

    # Check for spaces
    if " " in email:
        return False

    # Basic pattern for valid email characters
    # Local part can contain letters, numbers, dots, plus, hyphens, underscores
    local_pattern = r"^[a-zA-Z0-9._+-]+$"
    # Domain part can contain letters, numbers, dots, hyphens
    domain_pattern = r"^[a-zA-Z0-9.-]+$"

    if not re.match(local_pattern, local) or not re.match(domain_pattern, domain):
        return False

    # Domain must contain at least one dot and have valid structure
    if "." not in domain:
        return False

    # Check that domain doesn't start or end with dot or hyphen
    if (
        domain.startswith(".")
        or domain.endswith(".")
        or domain.startswith("-")
        or domain.endswith("-")
    ):
        return False

    # Check that local part doesn't start or end with dot
    if local.startswith(".") or local.endswith("."):
        return False

    return True
