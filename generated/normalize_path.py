# MIT License
# Copyright (c) 2026 axiom
# See LICENSE file for details.

# AXIOM GENERATED - DO NOT EDIT
# This file was generated by Axiom from: normalize_path.axiom
# Spec: normalize_path v1.0.0
# To modify this code, edit the spec file and run 'axiom build'
import os


def normalize_path(path: str) -> str:
    if not path:
        return ""

    # Convert backslashes to forward slashes
    path = path.replace("\\", "/")

    # Use os.path.normpath to handle . and .. components and redundant separators
    normalized = os.path.normpath(path)

    # Convert backslashes back to forward slashes (Windows normpath might add them back)
    normalized = normalized.replace("\\", "/")

    # Handle trailing slash removal for non-root paths
    if normalized != "/" and normalized.endswith("/"):
        normalized = normalized.rstrip("/")

    return normalized
